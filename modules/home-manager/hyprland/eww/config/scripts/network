#!/bin/sh

# Detect active ethernet via NetworkManager instead of interface name guesses.
eth_iface=$(nmcli -t -f DEVICE,TYPE,STATE device status 2>/dev/null | awk -F: '$2 == "ethernet" && $3 == "connected" {print $1; exit}')
if [ -n "$eth_iface" ]; then
  eth=$(ip -4 -o addr show dev "$eth_iface" 2>/dev/null | awk '{sub(/\/.*$/, "", $4); print $4; exit}')
fi

# Detect connected wifi and signal strength. A connected interface without a
# visible signal still counts as wifi so the bar does not show the crossed icon.
wifi_iface=$(nmcli -t -f DEVICE,TYPE,STATE device status 2>/dev/null | awk -F: '$2 == "wifi" && $3 == "connected" {print $1; exit}')
if [ -n "$wifi_iface" ]; then
  wifi=$(nmcli -t -f IN-USE,SIGNAL device wifi list ifname "$wifi_iface" 2>/dev/null | awk -F: '$1 == "*" {print $2 / 100; exit}')
  wifi=${wifi:-0}
fi

# Return json
if [ -n "$eth" ]; then
  printf '{"network":"eth","ip":"%s"}\n' "$eth"
elif [ -n "$wifi_iface" ]; then
  printf '{"network":"wifi","conn":%s}\n' "$wifi"
else
  echo 'null'
fi
